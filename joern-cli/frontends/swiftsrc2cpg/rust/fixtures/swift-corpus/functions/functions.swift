func add(_ a: Int, _ b: Int) -> Int {
  return a + b
}

func greet(name: String, count: Int = 1) -> Int {
  return name.count + count
}

func labeled(from start: Int, to end: Int) -> Int {
  return end - start
}
